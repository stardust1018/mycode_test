#include <string>
#include <vector>

class Solution {
public:
    std::string longestCommonPrefix(std::vector<std::string>& strs);
    void GetLongestComPrefixStr(std::string &lStr, const std::string &rStr);
};